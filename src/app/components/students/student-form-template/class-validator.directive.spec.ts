import { ClassValidatorDirective } from './class-validator.directive';

describe('ClassValidatorDirective', () => {
  it('should create an instance', () => {
    // const directive = new ClassValidatorDirective();
    // expect(directive).toBeTruthy();
  });
});
